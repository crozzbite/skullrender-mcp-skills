{
  "name": "@skullrender/mcp-skills",
  "version": "1.0.0",
  "description": "MCP server to expose AI agent skills",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "skullrender-skills": "bundle/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild src/cli.ts --bundle --platform=node --format=esm --outfile=bundle/cli.js --packages=external",
    "dev": "tsx src/cli.ts",
    "inspect": "mcp-inspector dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "glob": "^13.0.1",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "pkg": {
    "targets": [
      "node18-win-x64"
    ],
    "outputPath": "bin"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
